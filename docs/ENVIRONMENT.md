# Environment

The client may use these public values when Supabase sync is enabled:

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`

The publishable key is safe for browser use when RLS is configured. Never place a service role key in this project.

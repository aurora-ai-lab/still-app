# Still web prototype

## Run

```bash
pnpm install
pnpm dev
```

Open http://localhost:3000. The prototype works offline after the first load and stores sessions in the browser.

Supabase values can be provided later through environment variables; the current UI deliberately does not invent global aggregate data.
